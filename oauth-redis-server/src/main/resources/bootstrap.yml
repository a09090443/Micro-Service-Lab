spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: oauth-redis-server
  cloud:
    config:
      profile: dev
      uri: http://127.0.0.1:8000
#      label: develop

server:
  port: 8002
