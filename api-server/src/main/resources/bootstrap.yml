spring:
  application:
    name: api-server
  mvc:
    locale: zh_TW
  messages:
    basename: i18n.messages
    cache-duration: 1
    encoding: UTF-8
  cloud:
    config:
      profile: dev
      uri: http://127.0.0.1:8000
#      label: develop

server:
  port: 8011
