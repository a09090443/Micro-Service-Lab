type Query {
    findAllProducts(page:Int, limit:Int): [Product]
}

type Mutation {
    insertProduct(input: ProductInput): String!
}

type Product {
    name: String!
    price: Int!
    inventory: Int!
}

input ProductInput {
    name: String!
    price: Int!
    inventory: Int!
}
