server:
  port: 8080

security:
  jwt:
    public-key: classpath:jwt/public.txt

spring:
  jackson:
    serialization:
      INDENT_OUTPUT: true

# Management
management:
  endpoints:
    web.exposure.include: "*"
  endpoint:
    health:
      show-details: ALWAYS
